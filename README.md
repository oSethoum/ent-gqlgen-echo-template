# ent-gqlgen-echo-template

## generate ent

make sure ent is installed and added to the path.

```sh
go generate ./...
```

## generate graphql

- make sure gqlgen is installed and added to the path

```
gqlgen
```
